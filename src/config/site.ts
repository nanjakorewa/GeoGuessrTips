/**
 * Site-wide feature toggles.
 * Change these values to show/hide optional sections across the site.
 */
export const siteConfig = {
  /** 関連グッズ (BOOTH merchandise) section on country pages */
  showMerchandise: false,
};
